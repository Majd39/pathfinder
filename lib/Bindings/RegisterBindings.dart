import 'package:get/get.dart';

// import '../view/Register/RegisterController.dart';

class Registerbindings extends Bindings {
  @override
  void dependencies() {
    // Get.lazyPut<Registercontroller>(() => Registercontroller());
  }
}
